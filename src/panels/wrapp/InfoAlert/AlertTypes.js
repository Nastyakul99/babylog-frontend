import { INFO } from "../../../images"
import { ERROR } from "../../../images"

export const ALERT_TYPES = Object.freeze({
    ERROR: {
        img: ERROR
    },
    INFO: {
        img: INFO
    },
})